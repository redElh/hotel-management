/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['avatars.githubusercontent.com','images.unsplash.com','encrypted-tbn0.gstatic.com','plus.unsplash.com','www.ahstatic.com']
  }
};

export default nextConfig;
